pre-commit:
  commands:
    format:
      glob: "*.{ts,js,json}"
      run: bunx oxfmt --write {staged_files}
      stage_fixed: true
    lint:
      glob: "*.{ts,js}"
      run: bunx oxlint {staged_files}
