node_modules/

# Wrangler build output
.wrangler/
dist/

# Derived build artifact — regenerated by `bun run preprocess` from data/sources/*.json.
# predeploy hook rebuilds it before every wrangler deploy.
# First-time local setup: run `bun run preprocess` before `bun run start`.
data/asset-search-preprocessed.json

# Source asset data — large JSON files fetched from external APIs.
# Keep the folder with .gitkeep for local development.
data/sources/*.json
data/manifests/*.json
docs
# worker-configuration.d.ts is committed — regenerate with `bun run cf-typegen` after
# changing wrangler.jsonc bindings. It is a type declaration, not a build artifact.

# R2 credentials (never commit)
.env
.env.local
.env*.local
.codex

# OS / editor
.DS_Store
*.swp
*.swo
